/*

// INPUT 

sp = keyboard_check_pressed(ord("D"));
kj = keyboard_check_pressed(vk_space);

// VERTICAL COLLISION 
if (place_meeting(x,y+vsp,objBuildBlock)
{
	while (!place_meeting(x,y+sign(vsp),objBuildBlock)) y+=sign(vsp);
	if sign(vsp) == 1)
	{
		grounded = true;
	}
	
	vsp = 0;
}
else 
{
	grounded = false; 
}
y += vsp;

// HORIZONTAL COLLISION 
if(place_meeting(x+hsp,y,objBuildBlock))
{
	yp = 0;
	while(place_meeting(x+hsp,y-yp,objBuildBlock)) && yp <= abs(1*hsp))
} 

*/